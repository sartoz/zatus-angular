import { ZatUiComponentDirective } from './zat-ui-component.directive';

describe('ZatUiComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new ZatUiComponentDirective();
    expect(directive).toBeTruthy();
  });
});

import { ZatComponentDirective } from './zat-component.directive';

describe('ZatComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new ZatComponentDirective();
    expect(directive).toBeTruthy();
  });
});
